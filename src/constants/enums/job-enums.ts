export enum JobTypeEnum {
  FULL_TIME = "full-time",
  PART_TIME = "part-time",
}

export enum DepartmentEnum {
  HR = "HR",
  IT = "IT",
  FINANCE = "Finance",
  MARKETING = "Marketing",
  SALES = "Sales",
}

export enum EmploymentTypeEnum {
  PERMANENT = "permanent",
  CONTRACT = "contract",
  TEMPORARY = "temporary",
}
